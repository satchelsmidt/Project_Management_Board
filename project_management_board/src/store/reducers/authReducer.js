const initState = {}

//first reducer we create is for authentication.
//function, takes in initial state (which we pass manually), and the action
const authReducer =(state = initState, action) =>{
    return state
}


export default authReducer